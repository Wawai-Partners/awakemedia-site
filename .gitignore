# Dependencies
node_modules/

# Build output
dist/
tsconfig.tsbuildinfo

# Editor / OS
.DS_Store
Thumbs.db
*.local

# Environment
.env
.env.*
!.env.example

# Large media. GitHub rejects any single file over 100 MiB. The transcoded
# hero video is small enough to commit directly; other/raw video stays excluded.
*.mp4
*.mov
*.webm
video/
!public/hero-fluid.mp4
